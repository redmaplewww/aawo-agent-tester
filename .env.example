# Optional OpenAI-compatible reasoning provider.
# Keep real values in a local, ignored .env.local or managed profile.
LLM_PROVIDER=openai-compatible
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=

# Optional AAWO package/runtime configuration is supplied by the installed AAWO environment.
